# Fonts

Self-hosted webfonts go here. Drop `.woff2` files in this folder and declare the
`@font-face` rules in `app/globals.css`.

The design system falls back to the system font stack when no files are present,
so the app renders correctly with this folder empty.
